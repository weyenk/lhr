---
interface Props {
  type: 'recipe' | 'article';
}

const { type } = Astro.props;
const label = type === 'recipe' ? 'Recipe' : 'Article';
---
<span class="post-tag inline-block bg-accent px-2 py-0.5 text-[10px] font-bold uppercase tracking-wide text-white">{label}</span>
