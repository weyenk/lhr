{
  "name": "lhr-office",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=@lhr/db && npm run build --workspace=@lhr/jobs && npm run build --workspace=@lhr/llm && tsc --noEmit -p tsconfig.json && node scripts/bundle.mjs",
    "test": "vitest run",
    "dev": "npm run build --workspace=lhr-authoring-mcp-server && npm run build --workspace=@lhr/db && npm run build --workspace=@lhr/jobs && npm run build --workspace=@lhr/llm && tsx watch --env-file-if-exists=../../.env scripts/dev.ts"
  },
  "dependencies": {
    "@lhr/db": "*",
    "@lhr/jobs": "*",
    "@lhr/llm": "*",
    "express": "^4.21.0",
    "lhr-authoring-mcp-server": "*"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^22.0.0",
    "@types/supertest": "^6.0.0",
    "esbuild": "^0.24.0",
    "supertest": "^7.0.0",
    "tsx": "^4.23.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
