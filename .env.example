PUBLIC_UMAMI_URL=https://umami.loveheatrelationship.com/script.js
PUBLIC_UMAMI_WEBSITE_ID=
PUBLIC_CONVERTKIT_FORM_ID=
GITHUB_TOKEN=
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
DATABASE_URL=
KEEPA_API_KEY=
AMAZON_ASSOCIATES_TAG=

# Only used by `npm run dev:office` (local lhr-office server). If left blank, the dev server
# defaults both to "dev" — set these only if you want different local credentials. Production
# sets its own real values as Vercel project env vars; leaving these blank there causes /status
# to 401 on every request, which is intentional (no default password in production).
STATUS_AUTH_USER=
STATUS_AUTH_PASSWORD=
