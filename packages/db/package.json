{
  "name": "@lhr/db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run build --workspace=@lhr/schemas && tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@lhr/schemas": "*",
    "pg": "^8.13.0",
    "@types/pg": "^8.11.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
